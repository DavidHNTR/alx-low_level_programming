#include <stdio.h>

/**
 * main - Program start
 *
 * Return : Always 0 (Success)
 */
int main () {

   char ch;

   for(ch = 'a' ; ch <= 'z' ; ch++)
   {
   putchar(ch);

   if(ch == 'z')
     putchar("\n";
   }

    return(0);
}
